apiVersion: v1
kind: Service
metadata:
  name: realtime-service
spec:
  type: NodePort
  selector:
    app: realtime-app
  ports:
    - port: 8080        # Service port (matches pod)
      targetPort: 8080  # Pod containerPort
      nodePort: 30007   # External NodePort
